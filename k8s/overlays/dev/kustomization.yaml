# dev (the default stage). Single replica; the image digest is pinned by CI on each promote-by-PR (#377) —
# the newTag line below is the seam CI rewrites to the signed digest.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
replicas:
  - name: app-bravo
    count: 1
images:
  - name: 829808296602.dkr.ecr.us-east-1.amazonaws.com/team-bravo/demo-web
    digest: sha256:a1bfaf866995ce178cdb065b1d9d1143d927537d108726cab0947c1c633996e3
